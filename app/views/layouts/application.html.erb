<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Learn Rails" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Learn Rails" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag 'vendor/modernizr' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      <%= render 'layouts/messages' %>
      <%= yield %>

        <hr>
        <div class="row">
            <div class="small-2 large-4 columns">A</div>
            <div class="small-4 large-4 columns">B</div>
            <div class="small-6 large-4 columns">C</div>
        </div>
        <hr>
        <button class="large alert button">Order more!</button>
        <button class="submit">Order more!</button>

    </main>
    <footer class="row">
        <div class="small-12 medium-4 columns">
            Copyall 2015
        </div>
        <div class="small-12 medium-8 columns">
            No rights reserved.
        </div>
    </footer>
  </body>
</html>
